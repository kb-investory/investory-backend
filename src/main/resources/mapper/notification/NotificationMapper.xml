<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.investory.notification.infra.mappers.NotificationMapper">

    <resultMap id="notificationRowResultMap" type="com.investory.notification.infra.entities.NotificationRow">
        <id column="notification_id" property="notificationId"/>
        <result column="user_id" property="userId"/>
        <result column="notification_type" property="notificationType"/>
        <result column="title" property="title"/>
        <result column="message" property="message"/>
        <result column="reference_id" property="referenceId"/>
        <result column="is_read" property="read"/>
        <result column="created_at" property="createdAt"/>
        <result column="read_at" property="readAt"/>
    </resultMap>

    <select id="findByUser" resultMap="notificationRowResultMap">
        SELECT notification_id, user_id, notification_type, title, message, reference_id, is_read, created_at, read_at
        FROM notifications
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="long">
        SELECT COUNT(*)
        FROM notifications
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
    </select>

    <select id="findById" resultMap="notificationRowResultMap">
        SELECT notification_id, user_id, notification_type, title, message, reference_id, is_read, created_at, read_at
        FROM notifications
        WHERE notification_id = #{notificationId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="notificationId">
        INSERT INTO notifications
            (user_id, notification_type, title, message, reference_id, is_read, created_at, read_at)
        VALUES
            (#{userId}, #{notificationType}, #{title}, #{message}, #{referenceId}, #{read}, #{createdAt}, #{readAt})
    </insert>

    <update id="update">
        UPDATE notifications
        SET is_read = #{read},
            read_at = #{readAt}
        WHERE notification_id = #{notificationId}
    </update>

    <delete id="deleteByUserId">
        DELETE FROM notifications
        WHERE user_id = #{userId}
    </delete>

</mapper>
