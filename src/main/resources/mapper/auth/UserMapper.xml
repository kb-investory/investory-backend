<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.investory.auth.infra.mappers.UserMapper">
    <resultMap id="userRowMap" type="com.investory.auth.infra.entities.UserRow">
        <id property="userId" column="user_id"/>
        <result property="socialProvider" column="social_provider"/>
        <result property="socialSubject" column="social_subject"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="withdrawnAt" column="withdrawn_at"/>
    </resultMap>

    <select id="findBySocialProviderAndSocialSubject" resultMap="userRowMap">
        SELECT user_id, social_provider, social_subject, nickname, email, status, created_at, updated_at, withdrawn_at
        FROM users
        WHERE social_provider = #{socialProvider}
          AND social_subject = #{socialSubject}
    </select>

    <select id="findByUserId" resultMap="userRowMap">
        SELECT user_id, social_provider, social_subject, nickname, email, status, created_at, updated_at, withdrawn_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.investory.auth.infra.entities.UserRow"
            useGeneratedKeys="true" keyProperty="userId" keyColumn="user_id">
        INSERT INTO users (social_provider, social_subject, nickname, email, status)
        VALUES (#{socialProvider}, #{socialSubject}, #{nickname}, #{email}, #{status})
    </insert>

    <update id="update" parameterType="com.investory.auth.infra.entities.UserRow">
        UPDATE users
        SET nickname           = #{nickname},
            email              = #{email},
            status             = #{status},
            updated_at         = NOW(),
            withdrawn_at       = #{withdrawnAt}
        WHERE user_id = #{userId}
    </update>

</mapper>
