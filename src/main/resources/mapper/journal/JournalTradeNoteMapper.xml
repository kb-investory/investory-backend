<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.investory.journal.infra.mappers.JournalTradeNoteMapper">

    <resultMap id="journalTradeNoteRowResultMap" type="com.investory.journal.infra.entities.JournalTradeNoteRow">
        <id column="journal_trade_note_id" property="journalTradeNoteId"/>
        <result column="journal_id" property="journalId"/>
        <result column="trade_id" property="tradeId"/>
        <result column="rationale_text" property="rationaleText"/>
        <result column="rationale_label_type" property="rationaleLabelType"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByTradeIds" resultMap="journalTradeNoteRowResultMap">
        SELECT
            journal_trade_note_id,
            journal_id,
            trade_id,
            rationale_text,
            rationale_label_type,
            created_at,
            updated_at
        FROM journal_trade_notes
        WHERE trade_id IN
        <foreach collection="list" item="tradeId" open="(" separator="," close=")">
            #{tradeId}
        </foreach>
    </select>

    <select id="findByJournalId" resultMap="journalTradeNoteRowResultMap">
        SELECT
            journal_trade_note_id,
            journal_id,
            trade_id,
            rationale_text,
            rationale_label_type,
            created_at,
            updated_at
        FROM journal_trade_notes
        WHERE journal_id = #{journalId}
    </select>

    <!-- upsert: trade_id 유니크 제약을 이용해 있으면 갱신, 없으면 생성. journal_trade_note_id/created_at은
         ON DUPLICATE KEY UPDATE 절에 없어 충돌 시 기존 값이 그대로 유지된다. -->
    <insert id="insertAll">
        INSERT INTO journal_trade_notes
            (journal_id, trade_id, rationale_text, rationale_label_type, created_at, updated_at)
        VALUES
        <foreach collection="notes" item="row" separator=",">
            (#{row.journalId}, #{row.tradeId}, #{row.rationaleText}, #{row.rationaleLabelType}, #{row.createdAt}, #{row.updatedAt})
        </foreach>
        ON DUPLICATE KEY UPDATE
            rationale_text = VALUES(rationale_text),
            rationale_label_type = VALUES(rationale_label_type),
            updated_at = VALUES(updated_at)
    </insert>

    <delete id="deleteByTradeIds">
        DELETE FROM journal_trade_notes
        WHERE trade_id IN
        <foreach collection="list" item="tradeId" open="(" separator="," close=")">
            #{tradeId}
        </foreach>
    </delete>

    <resultMap id="rationaleLabelCountRowResultMap" type="com.investory.journal.infra.entities.RationaleLabelCountRow">
        <result column="rationale_label_type" property="rationaleLabel"/>
        <result column="label_count" property="count"/>
    </resultMap>

    <!-- journal_trade_notes에는 user_id가 직접 없어 investment_journals와 JOIN해서 사용자 조건을 건다.
         journal_date(투자일지 대상 날짜, 매수 판단이 실제로 있었던 날짜) 기준으로 필터링하고
         rationale_label_type별로 GROUP BY 하여 건수만 집계한다 — rationale_text는 조회하지 않는다.
         rationaleLabel 프로퍼티는 String으로 받는다 — 대소문자 정규화나 enum 변환은 하지 않고
         원본 문자열을 그대로 반환한다(호출 측인 tendency 도메인의 책임). -->
    <select id="countRationaleLabelsByUserAndDateRange" resultMap="rationaleLabelCountRowResultMap">
        SELECT
        jtn.rationale_label_type AS rationale_label_type,
        COUNT(*) AS label_count
        FROM journal_trade_notes jtn
        JOIN investment_journals ij ON ij.journal_id = jtn.journal_id
        WHERE ij.user_id = #{userId}
        AND ij.journal_date BETWEEN #{startDate} AND #{endDate}
        GROUP BY jtn.rationale_label_type
    </select>

</mapper>
