package com.investory.principle.domain.constant;

// principle_set_items.principle_recommendation_id 존재 여부로 파생되는 값 — 별도 컬럼으로 저장하지 않는다.
public enum PrincipleOriginType {
    AI_RECOMMENDATION,
    DIRECT
}
